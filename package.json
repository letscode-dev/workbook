{
  "name": "workbook",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "commit": "git add . && git commit -m \"Bash Script Commit\" && git push origin head",
    "build-gh-pages": "gh-pages -d dist",
    "deploy": "npm run build-gh-pages && npm run commit"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
